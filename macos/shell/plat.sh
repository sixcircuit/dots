echo "~/term/macos/plat.sh"
echo "args: $1 $2"

# this gets set in the right place in xnix/shell/path.sh
# it needs to come before the general xnix path, but the rest of xnix needs to run first, so we handle the path part there.
# export PATH=$PATH:~/term/macos/bin

